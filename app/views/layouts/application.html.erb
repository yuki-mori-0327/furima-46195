<!DOCTYPE html>
<html>

<head>
  <title><%= content_for?(:title) ? yield(:title) : "Furima46195" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <!-- Importmap だけでJSを配信。controllersの include_tag は使わない -->
  <%= javascript_importmap_tags %>
</head>

<body>
  <%= yield %>
</body>

</html>